##Arrays and Iteration 

##What is an array? 
list
collection of data
objects => object that stores other object 
#can store integers, arrays, strings, hashes
#mutable => able to modify arrays and modify their elements

##How to initialize an array
-literal constructor: []
-class constructor: Array.new 
#How to read arrays

##How to modify an array 
-<< (shovel)
- shift
-unshift 
-pop
- push 
- reverse 
- join 
- delete 
- split
-include 

#Iterating over arrays 

enumerators: 
-.each => iterates over each element, return value => original array
-.each_with_index => iterates over each element, return value => original array
-.collect/.map => returns a new array with changed elements
